# Override endpoint, API key, and default model via .env (OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL).
endpoint_url = ""
api_key = "lemonade"
model_name = "default"
output_dir = "./output"
background_file = "./background.md"

[models]
ollama = ["llama3.2", "mistral", "qwen2.5"]
lemonade = ["default"]
